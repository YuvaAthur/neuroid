//matlab();
c = new LinkedList();

Task t = new Task() {
	void job(Object o) {
	    s = (Synapse)o;
	    ns = s.getSrcNeuroid();
	    if (ns.getTimeLastFired() >= 0) {
		//wplot(n,s.getId());
		c.add(s.getProfile().getWeightedPotentialPlot());
	    }
	}};
UninterruptedIteration.loop(n.getSynapses(), t);
//spike(n);
c.add(n.getProfile().getMembranePotentialPlot());
h = grapher.multiPlot("Synapse Activities of " + n + " with " + n.getConcept(), c, null);
